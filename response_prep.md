这篇论文的黑盒实验部分主要集中在 **5.3节 (Black-box Transferability)**，主要分为以下两个部分：

### 1. 黑盒实验部分
1. **攻击开源黑盒 MLLMs (5.3.1 Attacking Black-box MLLMs)**: 
   - **实验设置**：在 InstructBLIP, LLaVA-1.5, MiniGPT-4, Shikra 这四个开源多模态大模型中，选取其中一个作为**白盒替代模型 (Surrogate Model)** 来生成对抗性视觉输入（加噪图像），然后将其迁移攻击其他三个未知的目标模型。
   - **评估任务**：在图像描述 (Image Captioning) 和 问答 (QA) 任务上进行测试。结果显示，该攻击在不同架构的模型间具有很强的迁移性（例如在目标黑盒模型上，幻觉句子比例 HSR 提升最高达 10.90%，幻觉词汇比例 HWR 提升达 12.74%）。
2. **攻击闭源商业 API (5.3.2 Attacking Closed-source Commercial APIs)**:
   - **实验设置**：将替代模型上生成的对抗样本直接输入给当前最先进的商业多模态 API，包括 **GPT-4o, GPT-4o mini, Gemini 1.5 Pro 和 Gemini 1.5 Flash**。
   - **评估结果**：尽管商业 API 提供商可能部署了防御机制，且模型参数完全黑盒，该攻击依然能有效绕过过滤机制，在不降低模型回复整体质量的前提下，成功诱导商业 API 产生幻觉（例如增加错误的对象、属性或关系描述）。

### 2. 他的黑盒思路简述
作者的黑盒攻击思路主要依赖于 **对抗迁移性 (Adversarial Transferability)**，其核心逻辑和方法如下：

- **白盒替代模型生成**：攻击者在没有目标模型参数、结构或训练数据的情况下，利用本地的一个白盒替代模型（Surrogate Model）通过计算梯度来优化生成对抗图像（加上微小的对抗扰动）。
- **针对通用机制攻击（迁移性的来源）**：
  传统的文本对抗或依赖预定义回复的攻击往往会在不同模型间失效（过拟合于单一模型）。为了实现高黑盒迁移性，作者放弃了针对特定任务或固定文本模式的攻击，转而**直接操纵 Transformer 架构中共有的核心机制——注意力机制 (Self-Attention)**。
  - **诱导注意力汇聚 (Attention Sinks)**：通过联合优化注意力损失 ($L_{attn}$) 和特征嵌入损失 ($L_{emb}$)，迫使模型将注意力错误地集中在某些“Sink Token”上。
  - **注入全局误导信息**：扰动视觉输入的隐藏层特征，将误导性的全局视觉信息注入到模型接下来的生成步骤中。
- **实现“隐蔽”的高效迁移**：由于“注意力汇聚 (Attention Sink)”现象在不同架构的 MLLM（无论是开源大模型还是 GPT-4o 这样的商业模型）中普遍存在，针对这一底层共有缺陷生成的对抗扰动具有极强的泛化能力。因此，它能够“骗过”完全未知的黑盒系统，自发诱发模型输出不相关的幻觉内容，而又不会破坏语句本身的流畅度和高质量表现，从而也规避了商业平台的常规安全检测。
